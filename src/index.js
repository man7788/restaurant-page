import './style.css';
import { 
  creatPhoto, 
  creatTitle,
  createReview, } 
  from './intial-load';

function component() {
}

content.appendChild(creatTitle());
content.appendChild(creatPhoto());
content.appendChild(createReview());

